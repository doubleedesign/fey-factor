// @ts-nocheck

export default {
	TemplateContainer: {
		__resolveType: (container) => {
			// TODO: Add logic here to determine the subtype to return for the container type
		}
	},
	Template: {
		__resolveType: (template) => {
			// TODO: Add logic here to determine the subtype to return for the extended type
		}
	}
};
