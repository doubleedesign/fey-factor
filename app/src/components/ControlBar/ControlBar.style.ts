import styled from 'styled-components';
import { Container } from '../common.ts';

export const StyledControlBar = styled(Container)`
	display: flex;
	justify-content: space-between;
	align-items: flex-end;
	padding: 1rem 0;
`;
